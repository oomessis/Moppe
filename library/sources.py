class channel(object):
    """Social media channel we are following."""
    def __init__(self, name):
        self.name = name

    # Class that will have properties to identify a social media channel we are following, such as
    # url, tokens, secrets, information about activities to identify new activites etc...